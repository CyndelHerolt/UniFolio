{% extends 'base.html.twig' %}

{% if is_granted('ROLE_ETUDIANT') %}
{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Mail_perso</th>
                <td>{{ etudiant.mailPerso }}</td>
            </tr>
            <tr>
                <th>Mail_univ</th>
                <td>{{ etudiant.mailUniv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_edit_index') }}">back to list</a>

    <a href="{{ path('app_profil_edit_edit', {'id': etudiant.id}) }}">edit</a>

    {% elseif is_granted('ROLE_ENSEIGNANT') %}

        {% block title %}Enseignant{% endblock %}

        {% block body %}
        <h1>Enseignant</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enseignant.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ enseignant.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ enseignant.nom }}</td>
            </tr>
            <tr>
                <th>Mail_perso</th>
                <td>{{ enseignant.mailPerso }}</td>
            </tr>
            <tr>
                <th>Mail_univ</th>
                <td>{{ enseignant.mailUniv }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_profil_edit_index') }}">back to list</a>

        <a href="{{ path('app_profil_edit_edit', {'id': enseignant.id}) }}">edit</a>


{% endif %}
    {{ include('profil_edit/_delete_form.html.twig') }}
{% endblock %}
