<tr>
    <td>{{ this.etudiant.prenom }} {{ this.etudiant.nom }}</td>
    <td>
        {% for groupe in this.etudiant.groupe %}
            {% if 'TP' in groupe.libelle %}
                {{ groupe.libelle }}
            {% endif %}
        {% endfor %}
    </td>
    <td>hello</td>
{#    TODO:Lien vers biblio de portfolio et biblio de traces :) #}
    <td>
{#        {% for portfolio in this.etudiant.portfolios %}#}
{#            {% for page in portfolio.pages %}#}
{#                {% for trace in page.trace %}#}
{#                    {{ trace.titre }}#}
{#                {% endfor %}#}
{#            {% endfor %}#}
{#        {% endfor %}#}
    </td>
    <td>
{#        {% for portfolio in this.etudiant.portfolios %}#}
{#            {{ portfolio.intitule }}#}
{#        {% endfor %}#}
    </td>
</tr>
