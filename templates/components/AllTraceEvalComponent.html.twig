<div{{ attributes }}>

    <div class="row">
        <div class="accordion accordion-flush col-4" id="accordionFlushExample">
            <div class="accordion-item">
                <h2 class="accordion-header" id="flush-headingOne">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                            data-bs-target="#flush-collapseOne" aria-expanded="false"
                            aria-controls="flush-collapseOne">
                        Filtrer par compétences
                    </button>
                </h2>
                <div id="flush-collapseOne" class="accordion-collapse collapse" aria-labelledby="flush-headingOne"
                     data-bs-parent="#accordionFlushExample">
                    <div class="accordion-body row">
                        <div>
                            {% for ordre in 1..3 %}
                                <fieldset>
                                    BUT {{ ordre }}
                                    {% for competenceChoice in this.competences %}
                                        {% if competenceChoice.ordre == ordre %}
                                            <div style="display: flex" class="form-check">
                                                <input type="checkbox" id="{{ competenceChoice.id }}"
                                                       name="{{ competenceChoice.id }}"
                                                       data-model="selectedCompetences[]"
                                                       data-action="live#action"
                                                       data-action-name="changeCompetences"
                                                       value="{{ competenceChoice.id }}"
                                                       class="form-check-input">
                                                <label for="{{ competenceChoice.id }}" style="margin-left: 10px"
                                                       class="form-check-label">{{ competenceChoice.libelle }}</label><br>
                                            </div>
                                        {% endif %}
                                    {% endfor %}
                                    <hr>
                                </fieldset>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>

{#        todo: tout à revoir #}
        <div class="accordion accordion-flush col-4" id="accordionFlushExample">
            <div class="accordion-item">
                <h2 class="accordion-header" id="flush-headingTwo">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                            data-bs-target="#flush-collapseTwo" aria-expanded="false"
                            aria-controls="flush-collapseTwo">
                        Filtrer par groupes
                    </button>
                </h2>
                <div id="flush-collapseTwo" class="accordion-collapse collapse" aria-labelledby="flush-headingTwo"
                     data-bs-parent="#accordionFlushExample">
                    <div class="accordion-body row">
                        <div>
                            <fieldset>
                                {% for semestre in semestres %}
                                    {{ semestre.libelle }}
                                    {% for typeGroupe in semestre.typeGroupes %}
                                        {% for groupe in typeGroupe.groupes %}
                                            <div style="display: flex" class="form-check">
                                                <input type="checkbox" id="{{ groupe.id }}"
                                                       name="{{ groupe.id }}"
                                                       data-model="selectedCompetences[]"
                                                       data-action="live#action"
                                                       data-action-name="changeCompetences"
                                                       value="{{ groupe.id }}"
                                                       class="form-check-input">
                                                <label for="{{ groupe.id }}" style="margin-left: 10px"
                                                       class="form-check-label">{{ groupe.libelle }}</label><br>
                                            </div>
                                        {% endfor %}
                                    {% endfor %}
                                    <hr>
                                {% endfor %}
                            </fieldset>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="accordion accordion-flush col-4" id="accordionFlushExample">
            <div class="accordion-item">
                <h2 class="accordion-header" id="flush-headingThree">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse"
                            data-bs-target="#flush-collapseThree" aria-expanded="false"
                            aria-controls="flush-collapseThree">
                        Filtrer par étudiants
                    </button>
                </h2>
                <div id="flush-collapseThree" class="accordion-collapse collapse" aria-labelledby="flush-headingThree"
                     data-bs-parent="#accordionFlushExample">
                    <div class="accordion-body row">
                        <div>
                            <fieldset>
                                {% for etudiant in this.etudiants %}
                                    <div style="display: flex" class="form-check">
                                        <input type="checkbox" id="{{ etudiant.id }}"
                                               name="{{ etudiant.id }}"
                                               data-model="selectedCompetences[]"
                                               data-action="live#action"
                                               data-action-name="changeCompetences"
                                               value="{{ etudiant.id }}"
                                               class="form-check-input">
                                        <label for="{{ etudiant.id }}" style="margin-left: 10px"
                                               class="form-check-label">{{ etudiant.prenom }}</label><br>
                                    </div>
                                {% endfor %}
                                <hr>
                            </fieldset>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


</div>
