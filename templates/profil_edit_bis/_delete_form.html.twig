<form method="post" action="{{ path('app_profil_edit_bis_delete', {'id': enseignant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseignant.id) }}">
    <button class="btn">Delete</button>
</form>
