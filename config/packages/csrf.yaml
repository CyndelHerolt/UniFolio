# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection: true

    csrf_protection:
        stateless_token_ids:
            - authenticate
            - logout
